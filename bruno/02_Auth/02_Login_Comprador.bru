meta {
  name: Login comprador
  type: http
  seq: 2
}

post {
  url: {{apiUrl}}/auth/login
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "correo": "comprador@tianguisdigital.mx",
    "contrasena": "12345"
  }
}

script:post-response {
  if (res.status === 200) {
    bru.setEnvVar("token", res.getBody().token, { persist: true });
  }
}
